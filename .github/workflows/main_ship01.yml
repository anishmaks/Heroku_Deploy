# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - ship01

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://ship01.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_4330ba5a6a00491abf6531f71f88fa7d }}
        password: ${{ secrets.AzureAppService_ContainerPassword_02146f619d2746a28b9e1edeeba83df3 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: ship01.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_4330ba5a6a00491abf6531f71f88fa7d }}/ship_classify:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'ship01'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_4afc1cdf265547f384895572d4e3414a }}
        images: 'ship01.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_4330ba5a6a00491abf6531f71f88fa7d }}/ship_classify:${{ github.sha }}'